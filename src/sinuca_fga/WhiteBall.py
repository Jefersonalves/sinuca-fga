from FGAme import *

class WhiteBall(Circle):
    def __init__(self):
        self.texture = get_texture(path_or_texture)
        self.__crop(rect, bbox)
        self.__rescale(scale, resampling)
        super(Circle, self).__init__(pos=pos)

